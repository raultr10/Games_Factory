<UserControl x:Class="Games_Factory.Views.GameDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F0F2F5">

    <Grid Margin="30">

        <Border Background="White" CornerRadius="15" Padding="40">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Opacity="0.1" ShadowDepth="3"/>
            </Border.Effect>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" CornerRadius="10" ClipToBounds="True" Margin="0,0,40,0" VerticalAlignment="Center">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="10" Opacity="0.2" ShadowDepth="2"/>
                    </Border.Effect>
                    <Image Source="{Binding SelectedGame.Producto.Imagen, Converter={StaticResource UrlImageConverter}}" 
                           Stretch="Uniform"/>
                </Border>

                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0">
                        <Button Content="🡠 Volver a Videojuegos" Command="{Binding GoBackCommand}" 
                                HorizontalAlignment="Left" Background="Transparent" BorderThickness="0" Margin="0,0,0,20" Foreground="#555" FontSize="16" FontWeight="SemiBold" Cursor="Hand"/>

                        <Viewbox HorizontalAlignment="Left" MaxHeight="45" Margin="0,0,0,10">
                            <TextBlock Text="{Binding SelectedGame.Producto.Nombre}" FontWeight="Black" Foreground="#333" TextWrapping="NoWrap"/>
                        </Viewbox>

                        <WrapPanel Orientation="Horizontal" Margin="0,5,0,10">
                            <Border CornerRadius="5" Padding="12,6" Margin="0,0,10,0">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#EEE"/>
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SelectedGame.TipoConsola}" Value="">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding SelectedGame.TipoConsola}" Value="{x:Null}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding SelectedGame.TipoConsola}" FontWeight="Bold" FontSize="14" Foreground="#000"/>
                            </Border>

                            <Border CornerRadius="5" Padding="12,6" Margin="0,0,10,0">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#FFEBEE"/>
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SelectedGame.Categoria}" Value="">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding SelectedGame.Categoria}" Value="{x:Null}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding SelectedGame.Categoria}" FontWeight="Bold" FontSize="14" Foreground="#D32F2F"/>
                            </Border>
                        </WrapPanel>

                        <TextBlock Text="{Binding SelectedGame.Producto.Precio, StringFormat='{}{0:N2} €'}" FontSize="36" FontWeight="Black" Foreground="#D32F2F" Margin="0,0,0,10"/>
                    </StackPanel>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,5,0,10" MinHeight="80">
                        <TextBlock Text="{Binding SelectedGame.Producto.Descripcion}" TextWrapping="Wrap" FontSize="16" Foreground="#666" LineHeight="26" Margin="0,0,20,0" TextAlignment="Justify"/>
                    </ScrollViewer>

                    <StackPanel Grid.Row="2">
                        <Separator Margin="0,5,0,15"/>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <TextBlock Text="Cantidad:" FontSize="16" FontWeight="Bold" Foreground="#333" VerticalAlignment="Center" Margin="0,0,15,0"/>
                            <TextBox Text="{Binding Cantidad}" Width="70" Height="40" FontSize="18" FontWeight="Bold" TextAlignment="Center" VerticalContentAlignment="Center">
                                <TextBox.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="8"/>
                                    </Style>
                                </TextBox.Resources>
                            </TextBox>
                        </StackPanel>

                        <WrapPanel Orientation="Horizontal">

                            <Button Command="{Binding AddToCartCommand}" Height="45" Padding="25,0" Margin="0,0,15,15" Cursor="Hand">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="#444"/>
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                                        <TextBlock Text="AÑADIR AL CARRITO" FontWeight="Bold" FontSize="14" Foreground="{TemplateBinding Foreground}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#222"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>

                            <Button Command="{Binding BuyNowCommand}" Height="45" Padding="25,0" Margin="0,0,0,15" Cursor="Hand">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="#D32F2F"/>
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                                        <TextBlock Text="COMPRAR AHORA" FontWeight="Bold" FontSize="14" Foreground="{TemplateBinding Foreground}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#B71C1C"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>

                        </WrapPanel>
                    </StackPanel>

                </Grid>
            </Grid>
        </Border>
    </Grid>

</UserControl>
