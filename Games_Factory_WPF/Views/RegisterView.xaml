<UserControl x:Class="Games_Factory.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="500">
    <Grid Background="#E0E0E0">
        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
            <Border Background="White" Width="450" CornerRadius="15" Padding="30" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" Opacity="0.2"/>
                </Border.Effect>

                <StackPanel>
                    <TextBlock Text="Crear Cuenta" FontSize="26" FontWeight="Bold" Foreground="#333" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="DNI *" Foreground="#666" FontWeight="Bold"/>
                            <TextBox Text="{Binding Dni}" Height="30" VerticalContentAlignment="Center" Margin="0,5,0,15"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Nombre *" Foreground="#666" FontWeight="Bold"/>
                            <TextBox Text="{Binding Nombre}" Height="30" VerticalContentAlignment="Center" Margin="0,5,0,15"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Apellidos" Foreground="#666"/>
                    <TextBox Text="{Binding Apellidos}" Height="30" VerticalContentAlignment="Center" Margin="0,5,0,15"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Correo Electrónico *" Foreground="#666" FontWeight="Bold"/>
                            <TextBox Text="{Binding Correo}" Height="30" VerticalContentAlignment="Center" Margin="0,5,0,15"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Teléfono" Foreground="#666"/>
                            <TextBox Text="{Binding Telefono}" Height="30" VerticalContentAlignment="Center" Margin="0,5,0,15"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Fecha de Nacimiento" Foreground="#666"/>
                    <DatePicker SelectedDate="{Binding FechaNacimiento}" Height="30" VerticalContentAlignment="Center" Margin="0,5,0,15"/>

                    <TextBlock Text="Dirección Completa" Foreground="#666"/>
                    <TextBox Text="{Binding Direccion}" Height="30" VerticalContentAlignment="Center" Margin="0,5,0,15"/>

                    <TextBlock Text="Código Postal" Foreground="#666"/>
                    <TextBox Text="{Binding CodigoPostal}" Width="100" HorizontalAlignment="Left" Height="30" VerticalContentAlignment="Center" Margin="0,5,0,15"/>

                    <TextBlock Text="Contraseña *" Foreground="#666" FontWeight="Bold" Margin="0,10,0,0"/>
                    <PasswordBox x:Name="txtRegPass" Height="30" Margin="0,5,0,25" VerticalContentAlignment="Center"/>

                    <Button Content="REGISTRARSE" Command="{Binding RegisterCommand}" CommandParameter="{Binding ElementName=txtRegPass}"
                            Style="{StaticResource BtnRojo}" Height="45" FontSize="16"/>

                    <TextBlock HorizontalAlignment="Center" Margin="0,20,0,0">
                         <Hyperlink Command="{Binding GoBackCommand}" TextDecorations="None">
                             <Run Text="🡠 Volver al Inicio de Sesión" Foreground="#666"/>
                         </Hyperlink>
                    </TextBlock>

                    <TextBlock Text="* Campos obligatorios" Foreground="#999" FontSize="12" HorizontalAlignment="Center" Margin="0,20,0,0"/>
                </StackPanel>
            </Border>
        </ScrollViewer>
    </Grid>
</UserControl>
